//과자가격,과자개수,현재가진돈 입력 -> 모자란 금액 출력 
#include<stdio.h>
int main(){
	int N, K, M; //과자 가격 K, 과자 개수 N, 현재 가진 돈의 액수 M
	int price, number, amount;
	scanf_s("%d", &price); //과자가격
	scanf_s("%d", &number); //과자개수
	scanf_s("%d", &amount); //현재 가진 돈
	if (price * number < amount)
		printf("0");
	else
		printf("%d", price * number - amount);
}
