//배수와 약수(5086번)-while(1)이게 생각이 잘 안남

#include<stdio.h>

int main() {
	int a, b;
	while (1) {
		scanf_s("%d %d", &a, &b);
		if ((a == 0) && (b == 0))
			break;
		else if (a % b == 0)
			printf("multiple"); //배수
		else if (b % a == 0)
			printf("factor"); //약수
		else
			printf("neither");
	}
	return 0;
} 
